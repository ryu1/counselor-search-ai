# AGENTS.md（プロジェクトメモリ）

カウンセラー検索AIデモシステムの開発環境です。会員の個人情報とカウンセリング相談内容を扱うため、セキュリティを最優先とします。

---

## ドキュメント参照ルール

作業に着手する前に、依頼の種類に応じて該当ドキュメントを読むこと。**推測で進めず、記載された規約と設計に従う。**

| 依頼の種類 | 参照先 |
|---|---|
| プロジェクト全体の把握 | `README.md`（概要・主な機能・ドキュメント目次） |
| ドキュメントの作成・更新 | `docs/documentation-guideline.md`（構成・記載ルール・完了チェックリスト） |
| コードレビュー | `docs/review.md`（レビュー観点・既知の課題） |
| 実装・修正 | `docs/conventions.md`（規約・命名・手順）、`docs/architecture.md`（構成・データモデル） |
| 認証・暗号化・権限に関わる変更 | `docs/security.md`（脅威モデル・設計方針） |
| 環境構築・デプロイ・障害調査 | `docs/conventions.md`、`docs/operations.md`、`docs/lambda-deployment.md` |
| UI・スタイルの変更 | `docs/design.md`（カラー・タイポグラフィ・ブレークポイント） |
| 要件・仕様の確認 | `docs/product.md`（要件・ユーザーストーリー・成功の定義） |
| 技術選定・依存ライブラリ | `docs/tech.md` |
| 用語・命名の確認 | `docs/glossary.md` |

複数に該当する場合はすべて読むこと。

`README.md` はリポジトリの入口であり、設計ドキュメントの目次を兼ねます。プロジェクトの概要・主な機能・ドキュメント構成に影響する変更を行った場合は `README.md` も更新する。

---

## 必ず守ること

### ドキュメントの同期

ソースコードを変更した場合、**同じコミット**で対応する設計ドキュメントを更新する。後回しは禁止。

更新対象の判断基準と完了チェックリストは `docs/documentation-guideline.md` に定義されている。

**違反防止フロー:**

1. **着手前** — 変更がどのドキュメントに影響するか確認
2. **変更中** — コードとドキュメントを同時に変更
3. **コミット前** — 同一コミットにコードとドキュメントが含まれているか確認
4. **コミット時** — 「コード変更 + ドキュメント更新」を1つのコミットメッセージに記載

### セキュリティ

本プロジェクトには是正されていない重大な脆弱性が存在する（`docs/review.md` の X-1〜X-5）。実装前に該当箇所を確認し、同種の問題を新たに作らないこと。

特に以下を守る。

- 認証情報・暗号化キーをソースコードに記述しない
- SQL はパラメータ化を使用する（Athena の場合は `escape_sql()` を使用）
- 個人情報を保存する場合は暗号化する
- 出力時にエスケープする

### 検証

推測で完了を報告しない。以下を実際に確認する。

- コマンドやパスを記載する場合は実行して確かめる
- 設定値を記述する場合は該当ファイルの行を参照する
- 未検証の内容は「※推測」または「※要確認」と明記する

### ドキュメント更新時の確認

ドキュメントを更新する前には、`docs/documentation-guideline.md` の以下のセクションを必ず確認する。

1. **含めるべき内容** — 該当ドキュメントに記載すべき項目を確認
2. **ドキュメント完了チェックリスト** — 更新後の完了条件を確認

### 変更の限定

依頼された範囲を超えて変更しない。
